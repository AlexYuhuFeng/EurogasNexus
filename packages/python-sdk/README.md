# packages/python-sdk

Reserved for a future standalone Python SDK package.

The active SDK shell currently lives in `src/eurogas_nexus/sdk` and must call
the backend API only.

Do not move SDK packaging here until a dedicated SDK packaging milestone is
selected.

Read before SDK work:

- `docs/clients/SDK_CLIENT_DESIGN_SPEC.md`
- `docs/clients/CLIENT_API_CONTRACT.md`
- `docs/contracts/15_SDK_CLI_CONTRACT.md`
- `.agent/plans/SDK_M1_API_CLIENT_EXECPLAN.md`
