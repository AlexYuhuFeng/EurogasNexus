# packages

Future distributable packages live here.

## Reserved

- `python-sdk/`: future standalone Python SDK package.

The active SDK shell currently lives under `src/eurogas_nexus/sdk`. Move or
package it only under an explicit SDK milestone.
