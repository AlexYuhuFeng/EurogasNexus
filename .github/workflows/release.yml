name: Build and Release

on:
  workflow_dispatch:
    inputs:
      release_channel:
        description: Release channel label.
        type: choice
        required: true
        default: preview
        options:
          - preview
          - rc
          - stable
      prerelease:
        description: Mark the GitHub Release as a prerelease.
        type: boolean
        required: true
        default: true
      build_desktop:
        description: Also build Windows and Linux x64/ARM64 desktop packages.
        type: boolean
        required: true
        default: false

permissions:
  contents: write

concurrency:
  group: eurogas-nexus-release-${{ github.ref }}
  cancel-in-progress: false

env:
  NODE_VERSION: "24"
  PYTHON_VERSION: "3.11"

jobs:
  validate:
    name: Validate release candidate
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Python package
        run: python -m pip install -e ".[dev]"

      - name: Ruff
        run: ruff check .

      - name: Tests
        run: pytest -q tests

      - name: Import API
        run: python -c "from apps.api.main import app; print('app import ok'); print(len(app.routes))"

  web:
    name: Build Web client
    runs-on: ubuntu-latest
    needs: validate

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: clients/web/package-lock.json

      - name: Install Web dependencies
        working-directory: clients/web
        run: npm ci

      - name: Build Web client
        working-directory: clients/web
        run: npm run build

      - name: Package Web client
        shell: bash
        run: |
          mkdir -p release-assets
          tar -czf "release-assets/eurogas-nexus-web-${GITHUB_SHA::7}.tar.gz" -C clients/web/dist .

      - name: Upload Web release asset
        uses: actions/upload-artifact@v4
        with:
          name: release-web
          path: release-assets/*
          if-no-files-found: error

  desktop:
    name: Build desktop client (${{ matrix.artifact_label }})
    if: ${{ github.event.inputs.build_desktop == 'true' }}
    runs-on: ${{ matrix.os }}
    needs: validate
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            bundle: nsis
            artifact_label: windows-x64
            artifact_glob: clients/desktop/src-tauri/target/release/bundle/nsis/*.exe
          - os: ubuntu-latest
            bundle: deb
            artifact_label: linux-x64
            artifact_glob: clients/desktop/src-tauri/target/release/bundle/deb/*.deb
          - os: ubuntu-24.04-arm
            bundle: deb
            artifact_label: linux-arm64
            artifact_glob: clients/desktop/src-tauri/target/release/bundle/deb/*.deb

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Linux Tauri dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            curl \
            file \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libwebkit2gtk-4.1-dev \
            patchelf \
            wget

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: |
            clients/web/package-lock.json
            clients/desktop/package-lock.json

      - name: Set up Rust
        run: rustup toolchain install stable --profile minimal

      - name: Install Web dependencies
        working-directory: clients/web
        run: npm ci

      - name: Install desktop dependencies
        working-directory: clients/desktop
        run: npm ci

      - name: Build desktop bundle
        working-directory: clients/desktop
        run: npm run build -- --bundles ${{ matrix.bundle }}

      - name: Collect desktop assets
        shell: bash
        run: |
          mkdir -p release-assets
          cp ${{ matrix.artifact_glob }} release-assets/

      - name: Upload desktop release asset
        uses: actions/upload-artifact@v4
        with:
          name: release-desktop-${{ matrix.artifact_label }}
          path: release-assets/*
          if-no-files-found: error

  publish:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs:
      - web
      - desktop
    if: ${{ always() && needs.web.result == 'success' && (needs.desktop.result == 'success' || needs.desktop.result == 'skipped') }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Download release assets
        uses: actions/download-artifact@v4
        with:
          pattern: release-*
          path: release-assets
          merge-multiple: true

      - name: Create release metadata
        id: metadata
        shell: bash
        run: |
          SHORT_SHA="${GITHUB_SHA::7}"
          CHANNEL="${{ github.event.inputs.release_channel || 'preview' }}"
          DESKTOP_INCLUDED="${{ github.event.inputs.build_desktop || 'false' }}"
          TAG="v0.5-${CHANNEL}-${GITHUB_RUN_NUMBER}-${SHORT_SHA}"
          echo "tag=${TAG}" >> "${GITHUB_OUTPUT}"
          {
            echo "# Eurogas Nexus v0.5 preview (${TAG})"
            echo
            echo "- Commit: ${GITHUB_SHA}"
            echo "- Branch: ${GITHUB_REF_NAME}"
            echo "- Channel: ${CHANNEL}"
            if [[ "${DESKTOP_INCLUDED}" == "true" ]]; then
              echo "- Includes: Web build, Windows x64 NSIS installer, Linux x64 DEB package, and Linux ARM64 DEB package."
            else
              echo "- Includes: Web build. Desktop packaging was not requested for this run."
            fi
            echo
            echo "Runtime data remains DB-first through the backend API. Clients do not connect directly to PostgreSQL."
          } > release-notes.md

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          PRERELEASE="${{ github.event.inputs.prerelease || 'true' }}"
          RELEASE_FLAGS=()
          if [[ "${PRERELEASE}" == "true" ]]; then
            RELEASE_FLAGS+=(--prerelease)
          else
            RELEASE_FLAGS+=(--latest)
          fi
          gh release create "${{ steps.metadata.outputs.tag }}" \
            release-assets/* \
            --repo "${GITHUB_REPOSITORY}" \
            --title "Eurogas Nexus ${{ steps.metadata.outputs.tag }}" \
            --notes-file release-notes.md \
            "${RELEASE_FLAGS[@]}"
