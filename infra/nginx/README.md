# infra/nginx

Reserved for future reverse-proxy configuration templates.

Nginx templates should route clients to the backend API and static web assets
only after backend and web deployment milestones exist.
