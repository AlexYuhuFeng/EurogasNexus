# Security Acceptance Evidence

Generated by: `scripts/security/run_security_acceptance.py`

## Automated status

`PASS` for the current worktree (run date recorded by the script).

Checks:

- API import is DB/network safe.
- Public surface is bounded (86 paths) and `/api/workflows/*` is removed.
- Every public path resolves to a declared permission.
- Public and internal tokens fail closed when unconfigured.
- Identity API keys are hash-only and role precedence is enforced.
- Legacy public-token compatibility principal is OPERATOR.
- OIDC access-token validation fails closed when unconfigured.
- Private-network/VPN-only posture is retained.

## External review status

`BLOCKED`

External acceptance cannot be completed from a code worktree. Required before
removing the private-network/VPN-only posture:

1. Real deployment penetration test and dependency CVE re-run.
2. OIDC issuer/JWKS TLS review by the customer identity team.
3. Backup/restore and incident-response drill on the target deployment.
4. Security sign-off by the deployment owner.

Until all four are complete, server roles remain private-network/VPN-only.

## Posture switch

`EUROGAS_NEXUS_DEPLOYMENT_POSTURE=security_accepted` is a precondition, not a
sign-off. `public_network_deployment_allowed()` returns true only when the
posture is `security_accepted` and
`EUROGAS_NEXUS_SECURITY_ACCEPTANCE_EVIDENCE` points to an existing file.
