# Live PostgreSQL Policy Compatibility Note

This filename is retained temporarily for compatibility with older links.

Use the neutral operations document instead:

- `docs/operations/LIVE_POSTGRESQL.md`

The project no longer uses milestone-style `V1` wording as the product identity.
