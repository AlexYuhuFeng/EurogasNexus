# UI Debt Register

Status: **active work queue; not whole-product acceptance**

This register consolidates the actionable queue from the [post-CR15 UI
audit](POST_CR15_UI_AUDIT.md) and [component census](COMPONENT_CENSUS.md). It
does not duplicate their capture narrative or source inventory. The audit
contains evidence from multiple refs; the closure context is source-audit
baseline `9a84f5e`, latest functional ref `ba26fe2`, and the current candidate
checkpoint. No future commit SHA is implied. Functional closure does not close
separate layout, visual, or acceptance debt.

## Classification rules

- `P1 confirmed` requires a reproduced product behavior that violates an
  accepted in-scope requirement.
- `P1 candidate` means the observed behavior needs current interaction or
  result confirmation before severity is assigned. Wrong or confusing
  Scenario scope belongs here until confirmed.
- `P2` is triage priority, not user acceptance. No P2 item in this register is
  marked accepted.
- `Evidence gap` and `Required implementation` are not runtime severities.
  Missing fixtures, reruns, screenshots, or external gates MUST NOT be
  upgraded to P1 by themselves.
- CR14 and CR15 required UI workflows remain required implementation scope;
  they are not deferred or accepted as thin UI merely because backend routes
  exist.

## Closed functional fixes

| ID | Screen / workflow | Severity | Description | Reason deferred | Recommended solution |
| --- | --- | --- | --- | --- | --- |
| CLOSED-NAV-001 | Optimize -> Review; Decision Center; Strategy Backtest -> Decision -> Strategy | Closed functional; layout debt open | URL, tab, heading, content, Back/Forward, and gas-day handoffs were functionally corrected. | Functional closure is evidenced by the audit's 61 Web tests, build, and Edge assertions at `3dbee3a`; loading/empty captures do not close visual acceptance. | Preserve the route contract while completing whole-product shell, overflow, and populated-state review. |
| CLOSED-ROUTE-001 | Portfolio Overview, Routes, Scenario | Closed functional; warning presentation debt open | Route feasibility and resource-pool warning behavior were corrected, including explicit UNKNOWN and deduplicated option warnings. | Audit evidence records 58 Web tests, build, readiness, and fixed Portfolio captures at `192cb4a`; this does not certify live market data or all warning surfaces. | Keep server-derived feasibility and warning identity; converge warning placement, units, alignment, and cross-workspace aggregation separately. |
| CLOSED-RESOURCE-001 | Portfolio resource selection, Resources/Library, contract draft | Closed functional; layout debt open | Selected resource versus editable draft identity, persisted loading, unknown status, and save guarding were corrected. | `9a84f5e` evidence records 64 Web tests, build, paired EN/CN labels, and `ux01-resource-missing-1440.png`; visual density and clipped/layout issues remain separate. | Preserve the functional identity boundary and migrate the surrounding resource panels, controls, and empty states to the accepted layout contract. |

## Active product debt

| ID | Screen / workflow | Severity | Description | Reason deferred | Recommended solution |
| --- | --- | --- | --- | --- | --- |
| UX01-001 | Global shell, Network and all workspaces | Closed scoped shell overlap; broader UI debt open | Parent reviewed `ux01-shell-endpoint-banner-1440.png`, `ux01-shell-endpoint-banner-1920.png`, and `ux01-shell-endpoint-banner-1100.png`; shell rows and context-banner content do not overlap in those three required viewports. | Closure is limited to the structural overlap correction. UI/token, bilingual, native, populated-state, and whole-product acceptance remain open. | Preserve the normal-flow shell rows; separately review the broader Constitution/token migration, content density, i18n, native, and populated states. |
| UX01-TOKEN-001 | Shared shell/header; Market (`market`), Portfolio, Decision | Scoped foundation adopted; broader debt open | Parent reviewed `ux01-token-fresh-1440.png`, `ux01-token-fresh-cn-1440.png`, `ux01-marketcockpit-cn-1440.png`, `ux01-portfolio-cn-1440.png`, and `ux01-decision-optimize-cn-1440.png`. The single H1 correctly identifies the workspace/task. Reviewed values are 4px primary tabs/buttons/header select/status, 32px controls, and 6px banner. | Only the shell-token foundation is adopted. Broad panel styles were reverted; legacy tokens, headings, spacing, raw-code treatment, density, normal-runtime behavior, and full international QA remain open. Parent verified `npm --prefix clients/web test` (`90 passed, 0 failed`) and `npm --prefix clients/web run build` (exit 0; TypeScript + Vite, 135 modules, 1.29s; existing dynamic-import warning). | Preserve the shared H1/header and shell-token foundation; complete the remaining token migration and bounded EN/CN, populated-state, normal-runtime, keyboard/focus, and viewport review before broader acceptance. |
| UX01-002 | Network offline/degraded error state | Closed on browser evidence; screen-reader announcement open | The failure surface rendered raw internal loader keys and never read the safe machine codes the store recorded for every failure. It now renders one bounded structured summary: translated endpoint labels (29-key mapping with a generic fallback), the safe code per endpoint, at most five labelled detail rows with a "showing N of M" line, and the retry control reports its attempt count and last attempt time while disabling itself and exposing `aria-busy` during a retry. Raw loader keys, i18n key names and backend prose no longer reach the DOM on this surface, and the adjacent release-blocker list in RuntimeWorkspace no longer prints loader keys either. | Verified live with route interception (a hanging `/market/spreads` plus 503s elsewhere): six affected endpoints, five rendered rows, no leak of keys or prose, a forced second retry click did not increment the counter, and the failure surface never turned into a success. Geometry checked at 1440/1024/800/600 with no overlap or overflow. CI still has no browser harness, so geometry and `aria-live` rest on that ad-hoc pass, and screen-reader announcement is unverified. | Add the browser harness to CI, and review the announcement behaviour with a screen reader. |
| UX01-SCENARIO-001 | Portfolio -> Optimize/Review -> Scenario | Closed functional - wrong-route association only; browser evidence open | Parent visually reviewed `output/playwright/ux01-scenario-second-route-1440.png` for `public-route-ttf-local`: source resource-pool allocation sale `27.05`, resource volume `8000`, cost `n/a`; Kant-matched response `gross_sale=27.0472`, `totalcost=25.0219`, `allocated=8000`, with no recommendation response. The candidate helper matches carried `route_id` and explicit pool `option_id`/resource fallback. | Closure is limited to the source association defect. The `routeRecommendation` browser fixture is absent; the parent independently reports `npm --prefix clients/web test`: `73 passed, 0 failed`, including explicit default and ambiguous-pool cases. No backend/full-acceptance suite, degraded-state, or broader Scenario/layout acceptance is claimed. | Preserve the helper and closed navigation contract. Add the browser fixture later and verify selected-route label/economics against the exact response; do not reopen this specific functional closure for unrelated layout debt. |
| UX01-EXPOSURE-001 | Portfolio -> Exposure | Closed on evidence; visual acceptance open | The reproduced false zero was backend-origin: `summarize_portfolio` summed an empty snapshot list to `0` and the DTO totals were non-nullable, so "unknown" was inexpressible and the workspace printed `GBP 0` beside unavailable rows. The four totals are now `float | None` in the domain model, the API payload and the Python SDK mirror, an empty/degraded read returns `null` with a `VALUATION_EVIDENCE_MISSING` warning in both `data.warnings` and `meta.warnings`, the route forwards the summary warnings instead of dropping them, and the client renders an explicit unknown (the placeholder rows no longer hardcode `0 MWh`). A measured total of `0.0` still reports `0.0`. | Unit/API/SDK tests cover empty, orders-without-evidence, multi-snapshot, measured-zero and configured-but-empty reads; the rendered string itself was not re-captured in a browser and the reviewing model has no image input. | Capture the Exposure workspace against a configured-but-empty runtime and confirm the unknown rendering visually before the whole-product acceptance pass. |
| UX01-MARKET-VIEW-001 | Market primary landing view | Closed on browser evidence; visual and copy review open | The fused `overview` dashboard is no longer the mandatory landing task. `curves` (numeric) and `network` (map) are separate persistable views with a per-principal stored preference (`eurogas.marketView.v1`), resolution URL task > persisted > declared default, and a landing that keeps the task switcher available - for both views, so a preference for the map no longer strands the user on a page without the switcher. `overview` and `capacity` stay reachable as explicit opt-in tasks and never overwrite a stored view; map geometry, verified/indicative legends and numeric semantics are untouched. | Verified against a live runtime at 1440x900: deep link honoured after sign-in, preference round trip in both directions with the gas day and hub retained, and the automated axe sweep reports zero violations on the numeric view and on all 16 workspaces in both locales. The visual review is now complete for both views at 1440 in English and Mandarin: the task switcher (overview / curves and spreads / network / capacity and events) stays present on the map task too, the map renders a real basemap with its AMap attribution and a four-entry legend (pipes, LNG, interconnection points, hubs) plus the verified/indicative dash styles, and no capture shows overlap, clipping or a stretched layout. The map's empty state is recorded separately as UX-MAP-EMPTY-001. What remains open is copy review: the longer bilingual strings pass the parity gate and render without truncation, but no human has judged their wording, and CI still has no browser harness. | Add a browser harness to CI, review the new copy in both locales, and confirm whether the standalone Network workspace should expose the Market task switcher. |
| UX01-WARN-001 | Decision/Optimize/Portfolio warning surfaces | P2 candidate - confirm aggregation | Warning presence and scope differ between Overview, Routes, and Optimize/Review observations; affected route/option context may be lost. | Route warning logic was functionally corrected, but cross-workspace aggregation and current evidence are not complete. | Define one server-result-derived warning aggregation view with affected object, severity, source, freshness, and time basis; do not hide or duplicate warnings. |
| UX01-003 | Network candidate results | Closed on copy contract | The intraday feed no longer calls spread candidates "Executable" / "可执行". The EN/CN title now says "Spread candidates for review" / "价差候选（供复核）", while the existing per-candidate human-review disclaimer remains in place. | Copy-only correction: no calculation, ranking, route-cost, status, or action semantics changed. `marketCockpit.test.ts` pins both locales and rejects the execution wording. | Keep candidate language review-oriented; any future action wording must preserve ADR-0012's decision-support/no-execution boundary. |
| UX01-004 | Network resource rail; Portfolio panels; shared surfaces | P2 | Repeated gas day/product context, deeply framed panels, oversized headings, and explanatory prose reduce analytical density. | Functional fixes are closed; layout migration and visual review are separate work. | Apply one global context owner, accepted type/spacing/radius tokens, compact panels, and evidence adjacent to the affected result. |
| STRATEGY-EMPTY-001 | Strategy Lab -> Compare | Closed on state contract | An empty or single-run selection now classifies as `NOT_READY`, never `COMPARABLE`. The Compare workspace renders an explicit empty/not-ready state and tells the user to create/select at least two persisted backtest runs before compatibility is evaluated. | No synthetic strategy fixture or comparison result was introduced; compatibility semantics for two or more runs are unchanged. `strategyLab.test.ts` pins the zero/one-run boundary. | Keep `COMPARABLE` reserved for selections with at least two actual persisted runs. |
| AGENT-STATE-001 | Agent Research -> Agent Runs -> Observable Replay | P2 | Raw blocker codes omit affected-series context; objective visibility, timestamp basis, clipped model text, and nested framing are inconsistent. | The captured run is a blocked-data path with zero tool invocations; successful findings/review-pack behavior remains unverified. | Map codes to human-readable affected-series messages, show objective and local/UTC basis, preserve lineage and human gates, and keep raw codes available as bounded technical detail. |
| DATA-EVIDENCE-001 | System Sources/Runtime; Market/Capacity; Glossary; Review/Strategy | P2 candidate - evidence-led | Freshness, time basis, provenance, verified/indicative status, and restricted-state presentation vary by surface; glossary date controls lack visible timezone basis. | Source/runtime captures include stale or blocked providers and do not establish live pricing or whole-product acceptance. | Use shared evidence/status contracts with visible source, freshness (`Fresh`/`Late`/`Stale`/`Missing`), time basis, lineage, rights, and simulation labels. |
| TABS-I18N-001 | System tabs; local workspace tabs; EN/CN workflows | P2 candidate - coverage-led | Native-looking System tabs differ from styled local tabs; census records local tab groups that may omit shared keyboard semantics; bilingual parity remains unverified. | Source census and partial captures identify drift but do not prove every tab or locale failure. | Migrate eligible groups to `WorkspaceTabs`, preserve specialist semantics, run keyboard/focus and EN/CN parity checks, and record exact failures rather than infer them. |
| UX01-AUTH-001 | Global entry, sign-in screen, desktop startup | Closed on browser evidence; visual review open | The terminal no longer mounts before authentication. `/api/me` answers 401 (`{"error":"unauthenticated"}`) when no credential is presented, the store resolves identity before any workspace read, `AppShell` renders a dedicated sign-in screen (company SSO plus a development-only credential form) for every non-authenticated state, deep links and `popstate` stay gated, logout clears client-stored credentials and rewrites the URL, and the desktop shell reveals its window only after the Web app reports identity resolution. Just-in-time SSO provisioning registers a `PENDING` principal instead of granting access, and the pending registration is committed before the request fails closed. | Verified live against the seeded PostgreSQL runtime: the unauthenticated entry renders sign-in copy only (no market or workspace panel), the development credential login then mounts the terminal with the deep link preserved, an authenticated CSRF-protected mutation succeeds through the same-origin proxy, and the axe sweep reports zero violations on the sign-in screen in both locales. The visual review is now complete for the entry screen: at 1440x900 and 1920x1080 the card keeps a fixed width, stays centred without clipping in English and Mandarin, offers the company SSO button plus the development credential form when the backend is reachable, and renders the fail-closed variant (error copy, SSO "not configured" note, no credential form at all) when the backend cannot be reached. One observation from that pass is deferred rather than fixed: the secondary "check identity again" control sits below the form as a narrow button among full-width ones (UX-AUTH-RETRY-001). The desktop shell's post-logout WebView cache clear is implemented and type-checked, not exercised at runtime (it needs a packaged build), CI still has no browser harness, and the desktop shell was type-checked with the machine's MSVC toolchain because the pinned GNU toolchain lacks `dlltool` locally. | Exercise the desktop cache clear in a packaged build and add the browser harness to CI. |

## Required implementation scope

### Market overview findings (2026-09-09)

At `eaf68f4`, parent reviewed `ux01-after-index-market-1440.png` against the
running PostgreSQL-backed application. The quote board is only 190px wide,
clipping six columns while the map occupies the primary analytical surface.
This violates the overview hierarchy; Network remains the map-first task.
The source also confirms a P1 price-basis defect: fallback spreads subtract
quotes without checking currency, unit, product, or delivery interval and label
the result GBP/MWh. A last trade is also displayed under a mid-price heading.
The scoped correction removes the unsafe spread arithmetic and uses matching
unexpired backend opportunity values with their comparison units. Quote rows
are primary-width, flat, and readable in parent-reviewed populated EN 1440 and
1920 captures. Parent verified 104 Web tests and a production build. This
closes the specific fallback-arithmetic and clipped-board defects, not the
full Market workflow: visible product/basis, bilingual metadata, accessible
table semantics, source-state correctness and endpoint latency remain open.

| ID | Screen / workflow | Severity | Description | Reason deferred | Recommended solution |
| --- | --- | --- | --- | --- | --- |
| CR14-UI-001 | Research Data -> datasets/features/targets -> dataset/artifact detail | Closed on live validation evidence; build-success and granted-export branches untested | Dataset selection, snapshot detail, quality, lineage, rights and truthful export-reference requests landed in 895e14d, and the governed spec/build surface now exists: a typed spec form omitting empty fields, a Validate action rendering registry resolution, spec hash, issue count and every structured issue grouped by field with its safe code, a Build action locked to the exact validated spec fingerprint and re-locked on any edit or failed validation, a created-snapshot card, and an artifact-delivery section derived from the server answer (restricted answers remove the request control and explain the governed state; unregistered formats are dropped; missing files are distinguished from unregistered formats; a granted answer renders the reference without a download link). | Verified live: validate rendered a real rejected spec with four `unknown_*` issues and build stayed locked. The build-success path cannot be reached on this runtime because the research registry is empty, and the granted-export branch is unreachable because governance grants no export clearance, so both are covered by model tests and by payloads shaped from the verified route contract rather than by a real run. | Seed the research registry on a test runtime to exercise a successful build end to end, and revisit the export control only if a source is ever granted clearance. |
| CR15-UI-001 | Agent Research -> governed research -> Review Pack -> Replay | Chain and gate rendered; real orchestrator run untested | The run replay now exposes the persisted artifact chain, and the workspace renders it: ordered entries with present/missing state, operation id, producing stage, artifact ids, replay identity (id, content hash, determinism), fixture identity, lineage (upstream artifacts, producing invocations, source families, series, snapshots, source references, evidence dependencies), entitlement state and policy boundary, timestamps, and each artifact's own payload - with an explicit "not persisted" note for absent artifacts and `hidden_chain_of_thought` shown only as the statement that nothing hidden is stored. A human gate records a verdict through the existing review-decision endpoint, disables its controls while in flight, localises refusals and re-reads the run so recorded decisions come from the backend. | Verified with a real confirmation POST whose decision a subsequent read returned, and with chain payloads stubbed to the committed backend shape, because this runtime has no agent run of its own. A real orchestrator-produced chain (and a decision echoed back into the replay payload) therefore remains unproven end to end; 403 refusal copy was observed with contract-shaped responses since the seeded identity is privileged. | Create one governed run through the workspace on a seeded runtime, open its replay, confirm the pack and re-read the decision from the payload. |

## Acceptance evidence gaps

### Research prerequisite audit (2026-09-14)

| ID | Screen / workflow | Severity | Description | Reason deferred | Recommended solution |
| --- | --- | --- | --- | --- | --- |
| CR14-RIGHTS-001 | Dataset build/detail/export | P1, coverage closed; export blocked on a rights decision | API and agent build pass authenticated scopes into pre-query canonical source filtering; runtime row/snapshot export policy is derived server-side; list/detail/quality/agent reads require persisted trusted source IDs and current grants; legacy/unknown provenance and forged export envelopes fail closed. The declared-but-unhandled `dataset.export` capability now has a handler reusing the same policy, and export matches the requested format against a stored artifact. `tests/integration/test_research_entitlement_postgres.py` now covers this against live PostgreSQL (isolated schema, wired into CI's PostgreSQL job) including the identity-negative 403s, forged `EXPORT_ALLOWED` envelopes at rest and in the body, the per-format artifact read path, and builder row-level filtering proven by a value switch. | Verified live, but the export endpoint currently cannot serve an artifact at all: the governance layer never yields a PUBLIC scope, no source definition sets `export_allowed`, so the resolver only produces `EXPORT_RESTRICTED`/`UNKNOWN` and the route answers 403 before the artifact lookup. That is fail-closed by construction and is a licensing/rights decision rather than a code gap. The real-build Parquet branch also remains uncovered because `pyarrow` is absent locally. | Decide whether any canonical source carries export clearance (and with which redistribution terms); until then keep the 403 and do not present the export route as a delivery path. Exercise the Parquet branch where `pyarrow` is installed. |
| CR14-SEMANTICS-001 | Dataset validate/build | Closed on tests | Validation did not resolve registry IDs, `entity_ids` was accepted without runtime filtering, and resampling used the first registry row while `bounded_resample` had no production caller - so a requested policy had no runtime effect. Validate and build now share one registry resolver that reports structured `{field, code, message}` issues, unknown IDs fail at validate time, built rows are filtered by `entity_ids`, and series values are produced through `bounded_resample` under the resolved policy with that policy recorded in the snapshot metadata. Unsupported policies are rejected with a structured error rather than ignored. | Not verified against live PostgreSQL; SQLite fixtures cover the behaviour. INTERPOLATE/mean-aggregation policies are rejected, not implemented. | Run the research suites against PostgreSQL when a live runtime is available in CI, and revisit the rejected aggregation policies only if a milestone requires them. |
| CR14-ARTIFACT-001 | Dataset export | Closed on tests | Builds persisted no artifacts and export could answer HTTP 200 with a null reference. Both build callers now write a CSV artifact (Parquet when pyarrow is importable) with a sha256 into a configurable artifact root, persist it before the snapshot and set the snapshot's `artifact_ref`; the export endpoint matches the requested format against a stored artifact and fails closed with 409 `artifact_not_available` (FORMAT_NOT_REGISTERED or FILE_MISSING) including the available formats. Stored references are relative to the root, so no host path leaks. | Verified with SQLite fixtures under a temporary artifact root; no live-PostgreSQL or real-deployment artifact test, and a failed commit after a write can leave an orphan file keyed by snapshot id. | Add a live-PostgreSQL artifact test, and decide whether orphan files should be reaped by a maintenance job. |
| UX-SYSTEM-NARROW-001 | Shared System navigation | Closed on measurement and visual review | At 390px the Mandarin System tabs wrapped per glyph into tall buttons. The task tabs are plain buttons inside the shared navigation primitive, and the CSS rules that were supposed to style them targeted a class the primitive never emits, so the labels fell back to UA styling. The rules now target the buttons in that container (and in the Access and Agents tab strips, which had the same gap), with `flex: 0 0 auto` and `white-space: nowrap`. | Re-measured and reviewed visually on the current build at 390x844 in Mandarin: all eight task tabs render 32px high with `white-space: nowrap` and `flex: 0 0 auto`, the strip's `scrollWidth` is 556 against a `clientWidth` of 358 so it scrolls horizontally, and the full-page capture shows a single-line strip with no per-glyph wrapping. The 1440 reference is the same 32px single line with no scrolling needed. The header's own height at 390px is a separate row (UX-NARROW-HEADER-001). | Keep EN/CN plus desktop checks in the whole-product acceptance pass. |

## Visual review findings (2026-09-14)

The agent's model now declares image input, so the captured screens were reviewed as images rather than inferred from the DOM. Two suspected copy defects were investigated and **disproved** - the market snapshot sentence reads "…are shown when sourced from EEX…" and every source-matrix row reads "Simulated source" in English; both looked wrong only in a downscaled preview. The rows below are what the pass actually found.

| ID | Screen / workflow | Severity | Description | Reason deferred | Recommended solution |
| --- | --- | --- | --- | --- | --- |
| UX-TOPBAR-IDENTITY-001 | Global header, identity cluster | Closed on visual evidence | The header rendered the signed-in user as one glued token - `dev.analystANALYST` - because `.topbar-user-menu` was used in the markup but had no CSS rule anywhere in the client, so the container never became a flex row and JSX dropped the whitespace between the display name and the role. | Fixed: the container now declares `display: flex; gap: 8px`, the name is the only flexible field (so a long one ellipsises instead of pushing sign-out off the row) and the role stays `nowrap`. Verified by re-capture: the measured gap between name and role is 8px and between role and button 8px, in English at 1440 and Mandarin at 390. `clients/web/tests/workspaceHeader.test.ts` now fails if the markup class loses its rule or the row stops separating the two fields. An axe sweep cannot see missing whitespace, which is why this needed a human-style visual pass. | Keep the container rule pinned; check the cluster again if a second identity field (for example an impersonation badge) is added. |
| UX-STATUS-BADGE-001 | Global header, data status | Closed on the UI constitution's status rules | The runtime database health badge was styled exactly like the two adjacent controls: same white pill, radius and border as the `EN` language select and the appearance select, and at ≤900px it stretched to a full row. Its healthy text was the subsystem name ("RUNTIME DB" / "运行数据库"), so nothing distinguished a health indicator from a dropdown - a reader could reasonably try to click it. | Fixed by applying the project's own rules instead of a restyle: UI constitution §9 requires the shell to own runtime/data status (so the slot stays), §15 mandates one operational vocabulary and forbids a synonymous label per screen, and §13 wants badges for state. The badge is now the canonical `StatusBadge` primitive rendering that vocabulary - Ready / Partial / Unavailable - with the store named only in the `title`, which §15 allows as the second signal. The mapping lives once in `app/model/dataPlaneStatus.ts` and is fail-closed (anything unrecognised reads Unavailable). The three surfaces that render this value - header, settings, market overview - now say the same words; the overview had been printing the raw enum. Sizing is pinned for both the flex row and the narrow two-column grid. Verified live: 1440 shows a 54px "Ready" chip with the store in its title, and 390x844 in Mandarin shows 38px - it measured 181px, half the row, before the grid sizing was pinned. | Keep new status surfaces on the shared vocabulary and never reintroduce the store name as a label. |
| UX-PREF-LANGUAGE-001 | Language preference | Closed on evidence | The interface language was not persisted: `i18n/index.ts` hard-coded `lng: "en"`, so a language chosen in settings died on every reload and the interface silently reverted to English. Theme already persisted, so the two preferences behaved differently for no stated reason. | Fixed: `src/i18n/language.ts` owns the storage key, the supported set and the coercion (`zh`, `zh-Hans` and `EN` all normalise; anything else falls back to English, and a storage that throws degrades to the in-session choice), and `changeAppLanguage` is the single writer, so no surface can switch language without storing it. Verified live: choosing Mandarin in settings and reloading leaves both `document.documentElement.lang` and `eurogas.language.v1` at `zh-CN`. | Keep the one-writer rule: a new surface that offers language must call `changeAppLanguage`. |
| UX-PREF-MENU-001 | Header preferences entry | P3 - deferred by the migration order | Language and appearance were removed from the header because §9 does not list preferences as shell global context, §13 sends option sets to menus and settings forms, and §22 prohibits equal-weight toolbar overload. The settings workspace owns both, so nothing is lost, but reaching them now costs two clicks (System then Settings) instead of one. | Deferred deliberately: §13 and §14 sanction a menu for option sets ("Menu for command grouping with keyboard navigation and focus return"), while §14 also states that no new overlay dependency is proposed and the census puts menus/overlays last in the migration order. Building one now would add a shared primitive ahead of that order. | When the menu/overlay phase starts, add one labelled preferences menu to the header - language, appearance, and the lifecycle actions §9 already asks to group - with keyboard navigation and focus return, and keep the settings form as the complete surface. |
| UX-MAP-EMPTY-001 | Market -> network task | P3 | With no persisted resource path the map renders a full-bleed basemap with a four-entry legend showing `0` for every node kind and no marker. The only explanation is outside the map, in the bottom strip (`resources 0/0`, `MARKET_PRICE_FX_UNAVAILABLE(EUR->GBP)`), so the empty map reads as a failed render rather than an honest absence. | The state itself is correct and fail-closed - the map does not fabricate a network - so this is a presentation gap, not a data defect. It needs a copy/design decision about where the explanation belongs. | Put the reason inside the map area as an empty state (what is missing, why, and what would populate it), keeping the FX/unavailability codes as bounded detail, and keep the legend visible with its zero counts. |
| UX-SOURCE-SIM-BADGE-001 | Market -> source matrix | P3 | Simulated provenance is plain grey body text ("Simulated source") in the same colour and weight as the factual cadence beside it, while the price cards mark the same sources with a distinct blue `EEX_SIM` pill. In a decision-support terminal the weaker treatment is on the surface that lists where every price came from. | Not changed here: the matrix deliberately avoids colour and the register already carries DATA-EVIDENCE-001 for provenance presentation across surfaces, so this belongs with that decision rather than in a visual-review patch. | Give simulated rows a shared simulation marker (badge or tinted row) matching the price-card treatment, and keep the wording explicit rather than relying on a `_SIM` suffix. |
| UX-NARROW-ALERT-001 | Global header at ≤900px | P3 | The alerts trigger hides its visible label at ≤900px (`span:not(.alert-trigger-mark) { display: none }`), leaving a dot and a bare count - "● 0" - with no visible meaning. The button keeps its `aria-label`, so assistive technology is unaffected; only sighted narrow-viewport users lose the label. | Not changed here: dropping the word is a deliberate space-saving choice for the narrow header and the accessible name is intact, so it is a copy/layout trade-off rather than a broken control. | Keep a short visible label at narrow widths (an icon plus "alerts", or the count with a glyph), or move the trigger into the workspace menu where it has room. |
| UX-NARROW-HEADER-001 | Global header at 390x844 | P3 | The header stacks into primary navigation, the context controls, the source-readiness block, the alerts and stream chips, the data-plane badge, the identity cluster and sign-out before the workspace content begins. Measured after the preference controls left the header: 328px of an 844px viewport. The header is not fixed or sticky, so nothing is occluded, but the first screen of a phone-sized view is still mostly chrome. | Partly addressed, not closed: removing the two preference selects (UX-PREF-MENU-001) cut the stack, and the earlier "roughly 450px" was an estimate from a capture rather than a measurement. What remains is a layout decision about which controls collapse behind a disclosure. | Collapse the context/status cluster behind one disclosure at narrow widths so the workspace title and first card stay above the fold, and re-measure rather than estimate. |
| UX-AUTH-RETRY-001 | Sign-in screen | P3 (cosmetic) | On the entry card the secondary "check identity again" control sits below the primary form as a narrow bordered button between full-width buttons, so it reads as an orphan rather than as the retry for the identity-resolution failure it belongs to. | Cosmetic only: the control works, is reachable, and its meaning is clear from the surrounding copy, so it did not justify a change inside the visual-review batch. | Move it next to the identity/SSO block it retries, or render it as an inline text action so the card has one visual weight per row. |

| ID | Screen / workflow | Severity | Description | Reason deferred | Recommended solution |
| --- | --- | --- | --- | --- | --- |
| EVID-DESKTOP-001 | Native WebView/Tauri desktop | Evidence gap | A native build at clean `9a84f5e` exists: 9,142,272 bytes with parent-reverified SHA-256 `124A148C6954F7536159F7DFBE413216C7E83F4A37EA203FBF831CCA6EDF41BC`. Startup/render evidence exists, but it is a baseline only and does not prove native interaction. | The older executable was correctly rejected as stale; rebuilding is not the next action. Native launch interaction, accessibility, and full page walkthrough against the 9a84f5e build remain pending. | Inspect the existing 9a84f5e artifact and record native screenshots, accessibility tree, navigation, stale/degraded states, and package identity; do not infer behavior from compilation or startup alone. |
| EVID-SUITE-001 | Whole-product Web and desktop acceptance | Evidence gap | Pending coverage includes browser/visual 1440x900, 1920x1080 and narrow desktop, EN/CN, keyboard/focus, long-session, populated research/agent workflows, rights negatives, and current-ref desktop checks. Parent verified `npm --prefix clients/web test` (`205 passed, 0 failed`), `npm --prefix clients/web run build` (exit 0; TypeScript + Vite, 148 modules; existing dynamic-import warning), and the bilingual i18n parity gate (`1644/1644`). The automated axe pass is now reproducible: a signed-in sweep of all 16 workspaces at 1440x900 reports `0` violations in English and `0` in Mandarin, and the sign-in screen, the numeric market view and the System workspace at 390x844 were audited separately. These are automated accessibility-tree results against a local runtime, not visual review: the captures have not been reviewed by a human, and 1920x1080, long-session, populated research/agent workflows and rights negatives remain unaudited. | The sweep compares rendered DOM against axe rules; it cannot detect visual or copy defects, and the harness lives in gitignored scratch rather than CI, so the run is reproducible locally but not enforced. Full-suite, full-auth, entitlement and whole-product acceptance remain open. A model visual review now complements the sweep: the agent's own model declares image input, so the captures are read rather than inferred. That pass reviewed the sign-in card (EN/zh, 1440 and 1920, reachable and unreachable backends), the numeric market view at 1440, the map task at 1440 in Mandarin, the System workspace at 390x844 in Mandarin, and the top bar; it found two defects (one fixed, one open as UX-STATUS-BADGE-001), four smaller observations, and disproved two suspected copy defects that turned out to be misreadings of a downscaled preview. It does not replace human review of wording and visual judgement, and it covered no long-session, populated research/agent or rights-negative state. | Move the sweep into CI as a browser job, keep capturing the listed states, and record exact commands, SHA, environment, fixture/operation IDs, screenshots, skips, and failures. Do not convert missing evidence into P1 or PASS. |
| EVID-FIXTURE-001 | Research, Agent, Market, Portfolio, Review/export | Evidence gap | Required fixtures for Unknown/Missing/zero-exposure, stale/fresh sources, restricted rights, simulation, MCP denial, replay, and export leakage are not all identified or rerun here. | No fixtures or pass claims are invented by this register. | Use existing labelled fixtures where available; record actual fixture IDs and exact results, and fail closed on missing rights/evidence. |
| RELIABILITY-READ-001 | Startup, retry, identity and user-triggered follow-up reads | Closed on reproduced evidence | Startup/retry `/me` generation, auth-denial, bounded lane, sign-in, source-failure, and cross-lane paths were already guarded, but the four user-triggered follow-up reads (`fetchGlossaryContext`, `askAnalysis`, `generatePortfolioReport`, `analyzeMonitoringAlert`) wrote their answer straight into the store without checking that the identity that asked was still current. They now capture the identity generation before starting, refuse to start during a sign-out, and drop a late answer on both the success and the failure path through one shared helper. | The defect was reproduced rather than reasoned about: with the local evidence proxy holding `POST /api/analysis/query` for five seconds and marking its body, a sign-out mid-flight followed by a sign-in rendered the marked answer - produced after the previous session was revoked - into the new session. The same probe without a sign-out renders the marker, so a "no marker" result means the guard worked and not that the probe was blind. The live A/B was run on `askAnalysis`; the other three reads share the same helper and are covered by `clients/web/tests/identityReadGuards.test.ts`, which fails if a post-await write is unguarded. | Keep the guard helper as the only path for new follow-up reads, and extend the same currency contract to the remaining mutation actions (provider credentials, review decisions, route and pool optimization, contract save) if they are ever shown to leak across identities. |

## References and ownership

- [Post-CR15 UI Audit](POST_CR15_UI_AUDIT.md) owns capture narrative and
  runtime evidence; this file owns prioritization and disposition.
- [Component Census](COMPONENT_CENSUS.md) owns source counts and primitive
  inventory; this file turns repeated gaps into work items.
- [CR1-15 Regression Matrix](CR1_15_REGRESSION_MATRIX.md) owns capability-level
  implementation/verification status.
- This register records prioritization, not whole-product acceptance. The
  PostgreSQL ordering index was applied and verified in `eaf68f4`; exact backup,
  migration, query-plan and 23-test evidence is in `UX01_EXECPLAN.md`.
  That indexed-query measurement does not close endpoint latency or visual debt.
